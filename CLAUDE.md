# CLAUDE.md

Read `tikal.yaml` at the repository root, then follow @ai-coding-tools/processes/getting-started.md. Load only the stack directories listed in `project.types` and that type's implied bases (`astro` always loads `typescript`).
